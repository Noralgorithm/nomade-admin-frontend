import { LoginCard } from "./_components/login-card";

export default function LoginPage() {
  return (
    <main className="flex min-h-screen w-full items-center justify-center px-3 bg-slate-800">
      <LoginCard />
    </main>
  );
}
